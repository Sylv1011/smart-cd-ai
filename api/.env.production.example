## API backend (FastAPI) - Production
ENVIRONMENT=production

# Production database URL (required in production)
PROD_DATABASE_URL=postgresql://username:password@prod-host:5432/postgres?sslmode=require

# Optional CORS allowlist
PROD_CORS_ALLOWED_ORIGINS=https://smartcd-ai.vercel.app

