# Set this to production on main branch deployments.
ENVIRONMENT=development

# Environment-specific database URLs:
# - ENVIRONMENT=production requires PROD_DATABASE_URL
# - ENVIRONMENT=staging requires STAGING_DATABASE_URL
# - ENVIRONMENT=development uses STAGING_DATABASE_URL or fallback DATABASE_URL
# Local override (used only when ENVIRONMENT=development and STAGING_DATABASE_URL is not set):
# DATABASE_URL=sqlite:///../test.db
PROD_DATABASE_URL=postgresql://username:password@prod-host:5432/postgres?sslmode=require
STAGING_DATABASE_URL=postgresql://username:password@staging-host:5432/postgres?sslmode=require

# CORS allowlists for frontend origins:
PROD_CORS_ALLOWED_ORIGINS=https://your-production-frontend.vercel.app
STAGING_CORS_ALLOWED_ORIGINS=https://your-staging-frontend.vercel.app
DEV_CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
