## Frontend (Vite)
# Environment label shown in UI (optional)
VITE_ENV_NAME=development

# Local ranking-engine (FastAPI) base URL
VITE_RANKING_API_URL=http://localhost:8001

# Optional: legacy backend base URL (not used by current flow; kept for fallback)
# VITE_API_URL=http://localhost:8000

# Optional: deployed AI explanation service base URL (Render)
# VITE_AI_LAYER_URL=https://smart-cd-ai-ai-explanation-service.onrender.com
